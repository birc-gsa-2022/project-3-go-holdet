/*
package main

import (
	"fmt"
	"strconv"
	"testing"
)

func TestOutput(t *testing.T) {

	genome, read := "aabaaab", "aa"

	lower, upper := RunSingle(genome, read)
	fmt.Println(lower, upper)

	if lower != 0 && upper != 5 {
		t.Error("expected 0,5 got" + strconv.Itoa(lower) + " " + strconv.Itoa(upper))
	}
}
*/